{% block form_row %}
    <div class="mb-3">
        {{ form_label(form, null, { label_attr: { class: 'form-label' } }) }}

        {{ form_widget(form, {
            attr: {
                class: 'form-control'
            }
        }) }}

        {{ form_errors(form) }}
    </div>
{% endblock %}
